---
- name: Install Nginx
  apt:
    name: nginx
    state: present

- name: Enable Nginx
  service:
    name: nginx
    enabled: yes
    state: started

- name: Copy Nginx configuration
  template:
    src: default.conf.j2
    dest: /etc/nginx/sites-available/default
  notify: Restart Nginx